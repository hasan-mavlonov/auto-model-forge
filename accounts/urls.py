# accounts/urls.py
from django.urls import path
from . import views

urlpatterns = [
    # временно пусто, потом добавим login/register
    # path("login/", views.LoginView.as_view(), name="login"),
]
